#include<stdio.h>
void main()
{
float a,b;
int sum;
printf("enter two floating point numbers\n");
scanf("%f%f",&a,&b);
sum=a+b;
printf("floating point numbers one is=%f\nfloating point numbers second is=%f\n",a,b);
printf("sum of the two values are=%d",sum);
}
